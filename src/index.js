import React from "react";
import ReactDOM from "react-dom";

import "./styles.css";
import AppExport from "./app";

const rootElement = document.getElementById("root");
ReactDOM.render(<AppExport />, rootElement);
